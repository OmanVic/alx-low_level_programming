#include "main.h"
/**
 * _isupper - check if an alphabet is uppercase
 *
 * @c: character to be tested
 *
 * Return: Return 1 if uppercase or ) otherwisw
 *
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
}
